# pip3 install folium == v

import folium

world_map = folium.Map()
# to show the map
world_map

# to center the map

world_map = folium.Map(location=[56.130, -156.35], zoom_start=4)


# ----------------------------------------------------------------------------------------------------------------------------------------------------------------
df_incidents = pd.read_csv("link to csv file or route ")  # noqa: F821

print("downloaded")

# top five of the dataframe
df_incidents.head()

# the size of the data
df_incidents.shape

limit = 100
# slice the df by index for flexibility we can also use the df.head(limit) and assign it
df = df_incidents.iloc[:limit, :]
df_incidents.shape


# -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

long = 37.77
lat = -122.42

san_map = folium.Map([lat, long], zoom_start=12)

incident = folium.map.FeatureGroup()

for lat, long in zip(df_incidents.Y, df_incidents.X):
    incidents.add_child(
        folium.features.CircleMarker(
            [lat, long],
            radius=5,
            color="yellow",
            fill=True,
            fill_color="blue",
            fill_opacity=0.6,
        )
    )
